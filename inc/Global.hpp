#pragma once

namespace gbl
{
    // Constants
    const int gridWidth = 40;  // Total cells in Width
    const int gridHeight = 30; // Total cells in Height
    const int cellSize = 20;   // 20x20 pixels for each cell
}